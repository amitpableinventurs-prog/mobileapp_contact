// The Laravel backend's LAN address, so a phone on the same Wi-Fi network
// (running this app in Expo Go) can reach it. Update this if your computer's
// IP address changes — find it with `ipconfig` (Windows) and look for the
// IPv4 Address under your Wi-Fi adapter.
//
// If you run an Android emulator on this same machine instead of a physical
// phone, use "http://10.0.2.2/laracontact/public/api" instead (10.0.2.2 is
// the emulator's alias for the host machine's localhost).
export const API_BASE_URL = 'http://192.168.1.35/laracontact/public/api';
